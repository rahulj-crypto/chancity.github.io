# Next.js Optimization Guide

## Image Optimization
- Use `next/image` for automatic resizing and lazy loading.

## Static Generation vs Server-side Rendering
- Prefer `getStaticProps` for marketing pages.
- Use `getServerSideProps` for user-specific data.

## Bundle Analysis
- Use `@next/bundle-analyzer` to track bundle size.
